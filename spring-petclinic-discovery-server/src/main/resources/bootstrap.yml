spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: discovery-server
---
spring:
  config:
    activate:
      on-profile: docker
  cloud:
    config:
      uri: http://localhost:3500/v1.0/invoke/config-server/method/ #http://config-server:8888
