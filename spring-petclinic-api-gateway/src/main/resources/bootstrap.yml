spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: api-gateway
---
spring:
  profiles: docker
  cloud.config.enabled: false
  application:
    name: api-gateway
eureka.client.enabled: false