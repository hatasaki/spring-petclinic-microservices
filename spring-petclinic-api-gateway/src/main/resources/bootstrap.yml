spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: api-gateway
---
spring:
  profile: docker
  cloud.config.enabled: false
  application:
    name: api-gateway
eureka.client.enabled: false